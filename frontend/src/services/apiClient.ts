/**
 * Central API client — JWT token management and fetch wrapper.
 * Re-exports from config/api.ts for backwards compatibility.
 */
export { API_BASE_URL, tokenStorage, apiFetch } from '../config/api';
